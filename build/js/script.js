document.addEventListener('DOMContentLoaded', function () {
    const formulario = document.querySelector('.formulario');
    const textoIngresado = document.querySelector('.formulario-propina');

    textoIngresado.addEventListener();

});